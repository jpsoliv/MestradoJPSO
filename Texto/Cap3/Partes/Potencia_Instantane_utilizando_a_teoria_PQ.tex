\section{Potência Instantânea Utilizando a Teoria PQ em Circuitos Trifásicos}\label{sec:Potência Instantânea Utilizando a Teoria PQ em Circuitos Trifásicos}

%\cite{Peng1996} \cite{Akagi1984}

A potência instantânea utilizando a teoria PQ é um conceito que utiliza de artifícios matemáticos para definir uma série de parâmetros presentes em circuitos elétricos trifásicos. A base da teoria consiste no estudo das tensões e correntes no domínio do tempo sem que haja restrição quanto ao balanceamento do sistema, à forma de onda e ainda podendo ser aplicada em regime transitório. Além disso, esta teoria pode ser utilizada tanto para circuitos trifásicos a três fios, quanto em circuitos a quatro fios com a presença de neutro. Uma das propostas concebida na utilização dessa teoria é a determinação das correntes circulantes do sistema as quais não transferem potências úteis à carga, fazendo desta uma poderosa ferramenta para ser utilizada no desenvolvimento de compensadores ativos para mitigar o efeito da circulação de potências não úteis do sistema. Com isso é possível conceber a eliminação de harmônicos, a compensação de reativos, ou mesmo a eliminação de desbalanços \cite{Akagi2007, AredesSD}.

Inicialmente tal teoria foi desenvolvida por Akagi \cite{Akagi1984}, a qual definiu as tensões e correntes de um sistema trifásico como sendo valores instantâneos quantificados em espaços vetoriais $\mathbf{v}$ e $\mathbf{i}$, respectivamente. Porém, em seu desenvolvimento inicial foi proposto a transformação do espaço vetorial nas coordenadas $abc$, a qual é definida pelo sistema de referência estacionário cujas tensões e correntes são defasadas em \ang{120} entre si, para as coordenadas $\alpha \beta 0$, a qual o sistema de referência estacionário é dado ortogonalmente entre si \cite{AredesSD}. Outra forma de tratar a definição de potência instantânea é trabalhando diretamente com o espaço vetorial nas coordenadas $abc$, sendo que as propriedades do sistema independem das coordenadas e são válidas tanto para o caso em que as definições são aplicadas em $abc$ como em $\alpha \beta 0$.

\subsection{Transformação de Coordenadas}

A teoria da potência instantânea PQ desenvolvida inicialmente por Akagi \cite{Akagi1984} emprega a transformação das coordenadas de um sistema com fases $abc$ para coordenadas $\alpha \beta 0$. O emprego dessa abordagem traz implicações de modo a poder introduzir sistemas trifásicos mais genéricos com a presença de sequência zero. Os resultados apresentados nessa abordagem apresentam as mesmas propriedades e interpretações de potência instantânea nas coordenadas $abc$, ou seja, estas independem do sistema de coordenada a qual é aplicada a teoria \cite{Peng1996}. 

\subsubsection{Transformada de Clarke}

A transformação de coordenadas das tensões instantâneas referenciadas no sistema $abc$ para tensões instantâneas referenciadas no sistema $\alpha \beta 0$ é feita através da aplicação da transformada de Clarke. Primeiramente considera-se que um sistema trifásico seja composto por tensões de fase $v_a$, $v_b$ e $v_c$ deslocadas em ângulo por \ang{120} ($\sfrac{2 \pi}{3}$) entre si. Desse modo, utilizando a equação \ref{eq:v_alpha_beta_0} obtém-se as novas tensões instantâneas $v_\alpha$ e $v_\beta$, cujo ângulo de defasagem entre as tensões no eixo $\alpha$ e $\beta$ é dado por \ang{90} ($\sfrac{\pi}{2}$).

\begin{equation}
\begin{bmatrix}
v_0\\
v_\alpha\\
v_\beta
\end{bmatrix}
= \sqrt{\dfrac{2}{3}}
\begin{bmatrix}
\dfrac{1}{\sqrt{2}}	& \dfrac{1}{\sqrt{2}}	& \dfrac{1}{\sqrt{2}}		\\[2ex]
1					& -\dfrac{1}{2}			& -\dfrac{1}{2}				\\[2ex]
0					& \dfrac{\sqrt{3}}{2}	& -\dfrac{\sqrt{3}}{2}
\end{bmatrix}
\begin{bmatrix}
v_a\\
v_b\\
v_c
\end{bmatrix}
\label{eq:v_alpha_beta_0}
\end{equation}

Ainda, por apresentar uma matriz invertível, é possível transformar um sistema referenciado em coordenadas $\alpha \beta 0$ para $abc$. Para a transformada inversa a relação a seguir é utilizada:

\begin{equation}
\begin{bmatrix}
v_a\\
v_b\\
v_c
\end{bmatrix}
= \sqrt{\dfrac{2}{3}}
\begin{bmatrix}
\dfrac{1}{\sqrt{2}}	&	1				& 0						\\[2ex]
\dfrac{1}{\sqrt{2}}	&	-\dfrac{1}{2}	& \dfrac{\sqrt{3}}{2}	\\[2ex]
\dfrac{1}{\sqrt{2}}	&	-\dfrac{1}{2}	& \dfrac{\sqrt{3}}{2}
\end{bmatrix}
\begin{bmatrix}
v_0\\
v_\alpha\\
v_\beta
\end{bmatrix}
\label{eq:vabc_inv}
\end{equation}

Do mesmo modo que ocorre nas tensões, a transformada de coordenada também pode ser feita para a corrente. Uma propriedade importante observada nessa transformação é que esta pode ser utilizada independentemente à forma da corrente, ou seja, esta pode conter conteúdo harmônico que a transformação ainda é válida. Sendo assim a equação \ref{eq:i_alpha_beta_0} pode ser utilizada.

\begin{equation}
\begin{bmatrix}
i_0\\
i_\alpha\\
i_\beta
\end{bmatrix}
= \sqrt{\dfrac{2}{3}}
\begin{bmatrix}
\dfrac{1}{\sqrt{2}}	& \dfrac{1}{\sqrt{2}}	& \dfrac{1}{\sqrt{2}}		\\[2ex]
1					& -\dfrac{1}{2}			& -\dfrac{1}{2}				\\[2ex]
0					& \dfrac{\sqrt{3}}{2}	& -\dfrac{\sqrt{3}}{2}
\end{bmatrix}
\begin{bmatrix}
i_a\\
i_b\\
i_c
\end{bmatrix}
\label{eq:i_alpha_beta_0}
\end{equation}

Já a transformada inversa de coordenadas para a corrente é dada segundo a equação \ref{eq:iabc_inv}

\begin{equation}
\begin{bmatrix}
i_a\\
i_b\\
i_c
\end{bmatrix}
= \sqrt{\dfrac{2}{3}}
\begin{bmatrix}
\dfrac{1}{\sqrt{2}}	&	1				& 0						\\[2ex]
\dfrac{1}{\sqrt{2}}	&	-\dfrac{1}{2}	& \dfrac{\sqrt{3}}{2}	\\[2ex]
\dfrac{1}{\sqrt{2}}	&	-\dfrac{1}{2}	& \dfrac{\sqrt{3}}{2}
\end{bmatrix}
\begin{bmatrix}
i_0\\
i_\alpha\\
i_\beta
\end{bmatrix}
\label{eq:iabc_inv}
\end{equation}

Um dos triunfos da transformada de Clarke é a separação das componentes de sequência zero das tensões e correntes ($v_0$ e $i_0$ respectivamente) expressas nas coordenadas $abc$, ou seja, os eixos $\alpha$ e $\beta$ não carregam contribuições da sequência zero.

Para melhor visualizar a transformada de Clarke em um sistema trifásico balanceado, as figuras \ref{fig:va_vb_vc_Clarke} e \ref{fig:valpha_vbeta_Clarke} mostram as tensões típicas de um sistema balanceado referenciado nas coordenadas $abc$ e sua equivalente transformada nas coordenadas $\alpha \beta 0$, respectivamente. Nessas figuras além das formas de onda em função do tempo, representadas pelas figuras \ref{fig:va_vb_vc.eps} e \ref{fig:alpha_beta.eps} há ainda os equivalentes fasoriais estacionários representados pelas figuras \ref{fig:va_vb_vc_fasor.png} e \ref{fig:valpha_vbeta_fasor.png}. Através da análise dessas figuras fica claro o modo que a transformada de Clarke muda a referência de um sistema trifásico com tensões $v_a$, $v_b$ e $v_c$ defasadas em \ang{120} para um sistema cuja representação $v_\alpha$ e $v_\beta$ estão defasadas em \ang{90}.

\begin{figure*}[!htb] %Circuito típico de um retificador de 12 pulsos com sua respectiva corrente de entrada
	\centering
	\begin{subfigure}[b]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Cap3/Figuras/va_vb_vc.eps}
		\caption{Tensões nas coordenadas $abc$} 
		\label{fig:va_vb_vc.eps}
	\end{subfigure}%
	%	\hfill
	\begin{subfigure}[b]{0.3\textwidth}  
		\centering 
		\includegraphics[width=\textwidth]{Cap3/Figuras/va_vb_vc_fasor.png}
		\caption{Fasor das tensões nas coordenadas $abc$}    
		\label{fig:va_vb_vc_fasor.png}
	\end{subfigure}%
	\caption{Sistema trifásico referenciado em coordenadas $abc$}
	\label{fig:va_vb_vc_Clarke}
\end{figure*}

\begin{figure*}[!htb] %Circuito típico de um retificador de 12 pulsos com sua respectiva corrente de entrada
	\centering
	\begin{subfigure}[b]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Cap3/Figuras/alpha_beta.eps}
		\caption{Tensões nas coordenadas $\alpha \beta$} 
		\label{fig:alpha_beta.eps}
	\end{subfigure}%
	%	\hfill
	\begin{subfigure}[b]{0.3\textwidth}  
		\centering 
		\includegraphics[width=\textwidth]{Cap3/Figuras/valpha_vbeta_fasor.png}
		\caption{Fasor das tensões nas coordenadas $\alpha \beta$}    
		\label{fig:valpha_vbeta_fasor.png}
	\end{subfigure}%
	\caption{Sistema trifásico referenciado em coordenadas $\alpha \beta 0$}
	\label{fig:valpha_vbeta_Clarke}
\end{figure*}

\subsection{Potência Instantânea em Coordenadas $\alpha \beta 0 $}

As potências instantâneas $p$ e $q$ podem ser calculadas independentemente das coordenadas definidas para as tensões e correntes. Nessa seção serão apresentadas as definições das potências considerando as coordenadas $\alpha \beta 0$. Primeiramente, tratando as tensões $v_{\alpha}$, $v_{\beta}$ e $v_0$ segundo o espaço vetorial definido por $\mathbf{v}=[v_{\alpha}\; v_{\beta}\; v_0]^T$ e, similarmente, definindo o espaço vetorial com as correntes $i_{\alpha}$, $i_{\beta}$ e $i_0$ no vetor $\mathbf{i}=[i_{\alpha}\; i_{\beta}\; i_0]^T$, a potência instantânea ativa $p$ pode ser definida segundo a equação \ref{eq:Palphabeta0}.

\begin{equation}	%\label{eq:V}
\begin{aligned}
p &= \mathbf{v} \cdot \mathbf{i}\\
p &= v_{\alpha}i_{\alpha}+v_{\beta}i_{\beta}+v_{0}i_{0}
\end{aligned}
\label{eq:Palphabeta0}
\end{equation}


Já a definição de potência reativa é descrita por um vetor composto pelos elementos $q_{\alpha}$, $q_{\beta}$ e $q_0$ na forma de $\mathbf{q} = [q_{\alpha}\; q_{\beta}\; q_0]^T$, a qual é definido segundo a expressão:

\begin{equation}	%\label{eq:V}
\mathbf{q} = \mathbf{v} \times \mathbf{i}
\label{eq:q}
\end{equation}

Ou seja, expandido a equação \ref{eq:q} define-se $\mathbf{q}$ como o vetor apresentado na expressão \ref{eq:q_expand} a seguir.

\begin{equation}
\mathbf{q} = 
\begin{bmatrix}
q_{\alpha}\\
q_{\beta}\\
q_0
\end{bmatrix}
=
\begin{bmatrix}
v_{\beta} i_0 - v_0 i_{\beta}\\
v_0 i_{\alpha} - v_{\alpha} i_0\\
v_{\alpha} i_{\beta} - v_{\beta} i_{\alpha}
\end{bmatrix}
\label{eq:q_expand}
\end{equation}

A norma do vetor $\mathbf{q}$ representa o valor total da potência instantânea conforme a expressão \ref{eq:qnorm}. Cabe lembrar os valores $q_{\alpha}$, $q_{\beta}$ e $q_0$ são variantes no tempo, fazendo com que o valor $q$ seja uma função no tempo da mesma forma que $p$.

\begin{equation}	%\label{eq:V}
q = |\mathbf{q}| = \sqrt{q_{\alpha}^2+q_{\beta}^2+q_0^2}
\label{eq:qnorm}
\end{equation}

Com a elucidação das potências instantâneas $p$ e $q$, define-se certos parâmetros para o entendimento das propriedades e significados físicos de tais potências. Para tal, determina-se as correntes ativas e reativas instantâneas segundo as expressões \ref{eq:i_p} e \ref{eq:i_q}, respectivamente. 

\begin{equation}
\mathbf{i_p} = 
\begin{bmatrix}
i_{\alpha p}\\
i_{\beta p}\\
i_{0 p}
\end{bmatrix}
\triangleq
\dfrac{p}{\mathbf{v}\cdot \mathbf{v}} \mathbf{v}
\label{eq:i_p}
\end{equation}

\begin{equation}
\mathbf{i_q} = 
\begin{bmatrix}
i_{\alpha q}\\
i_{\beta q}\\
i_{0 q}
\end{bmatrix}
\triangleq
\dfrac{\mathbf{q} \times \mathbf{v}}{\mathbf{v} \cdot \mathbf{v}}
\label{eq:i_q}
\end{equation}

Além disso, as expressões \ref{eq:S} e \ref{eq:lambda} são válidas para os valores encontrados na teoria das potências instantâneas, ou seja, $S=vi$ e $\lambda = \sfrac{p}{s}$. Conforme apresentado em \cite{Peng1996}, tais definições apresentam certos teoremas que demonstram algumas propriedades da teoria da potência instantânea.

A primeira propriedade importante advém da corrente $\mathbf{i}$ que percorre o sistema trifásico nas coordenadas $\alpha \beta 0$ como sendo a composição das correntes $\mathbf{i_p}$ e $\mathbf{i_q}$, ou seja, $\mathbf{i} \equiv \mathbf{i_p} + \mathbf{i_q}$. Tal demonstração dá-se pela expansão das expressões \ref{eq:i_p} e \ref{eq:i_q}, a qual é mostrada pela equação \ref{eq:i_p+i_q}. 

\begin{equation}
\mathbf{i_p} + \mathbf{i_q} 
=
\begin{bmatrix}
v_{\alpha} \left ( \dfrac{v_{\alpha} i_{\alpha} + v_{\beta} i_{\beta} + v_0 i_0}{v_{\alpha}^2  + v_{\beta}^2 + v_0^2}\right)\\[2ex]
v_{\beta} \left ( \dfrac{v_{\alpha} i_{\alpha} + v_{\beta} i_{\beta} + v_0 i_0}{v_{\alpha}^2  + v_{\beta}^2 + v_0^2}\right)\\[2ex]
v_0 \left ( \dfrac{v_{\alpha} i_{\alpha} + v_{\beta} i_{\beta} + v_0 i_0}{v_{\alpha}^2  + v_{\beta}^2 + v_0^2}\right)
\end{bmatrix}
+
\begin{bmatrix}
\dfrac{v_0(v_0 i_{\alpha}-v_{\alpha} i_0)-v_{\beta}(v_{\alpha} i_{\beta}-v_{\beta} i_{\alpha})}{v_{\alpha}^2  + v_{\beta}^2 + v_0^2}\\[3ex]
\dfrac{v_{\alpha}(v_{\alpha} i_{\beta}-v_{\beta} i_{\alpha})-v_0(v_{\beta} i_0-v_0 i_{\beta})}{v_{\alpha}^2  + v_{\beta}^2 + v_0^2}\\[3ex]
\dfrac{v_{\beta}(v_{\beta} i_0-v_0 i_{\beta})-v_{\alpha}(v_0 i_{\alpha}-v_{\alpha} i_0)}{v_{\alpha}^2  + v_{\beta}^2 + v_0^2}
\end{bmatrix}
= 	
\begin{bmatrix}
i_{\alpha}\\
i_{{\beta}}\\
i_{0}
\end{bmatrix} 
=	\mathbf{i}
\label{eq:i_p+i_q}
\end{equation}

Outra importante propriedade é dada pelo paralelismo entre os vetores $\mathbf{v}$ e $\mathbf{i_p}$ em todo espaço vetorial, ou seja, $\mathbf{v} \times \mathbf{i_p} \equiv 0$. Para demonstrar essa propriedade a equação \ref{eq:v_ip} pode ser analisada. Esse teorema tem um importante significado, a qual é a definição da corrente que se encarrega com a transferência da potência ativa instantânea entre subsistemas. Isto ocorre visto que a tensão e corrente instantâneas estão sob o mesmo eixo. Desta forma pode-se atribuir a dimensão de Watt (W) para a potência proveniente do produto entre $\mathbf{v}$ e $\mathbf{i_p}$.

\begin{equation}
\mathbf{v} \times \mathbf{i_p}	= \mathbf{v}\times \left( \dfrac{p}{\mathbf{v} \cdot \mathbf{v} } \mathbf{v} \right ) = \mathbf{0}
\label{eq:v_ip}
\end{equation}

Analogamente a propriedade explanada anteriormente, observa-se a ortogonalidade entre a tensão $\mathbf{v}$ e a corrente $\mathbf{i_q}$, ou seja, $\mathbf{v} \cdot \mathbf{i_q} \equiv 0$. A demonstração dessa propriedade pode ser analisada segundo a equação \ref{eq:v_iq}. Como explicado anteriormente, o produto entre as tensões e correntes reativas não proporcionam a transferência de potência entre a fonte e a carga. Desse modo a dimensão de tal potência não pode ser considerada W, VA ou VAr, sendo que os autores de tal teoria introduziram uma nova unidade: Volt-Ampere Imaginário (vai) \cite{Akagi1984}.

\begin{equation}
\begin{aligned}
\mathbf{v} \cdot \mathbf{i_q}	&= \mathbf{v}\cdot \left( \dfrac{\mathbf{q} \times \mathbf{v}}{\mathbf{v} \cdot \mathbf{v} } \right ) \\
&= \mathbf{v}\cdot \left( \dfrac{(\mathbf{v} \times \mathbf{i}) \times \mathbf{v}}{\mathbf{v} \cdot \mathbf{v} } \right )\\
&= \mathbf{v}\cdot \left( \dfrac{-(\mathbf{i} \cdot \mathbf{v}) \mathbf{v} + (\mathbf{v} \cdot \mathbf{v}) \mathbf{i}}{\mathbf{v} \cdot \mathbf{v}} \right)\\
&= \dfrac{-(\mathbf{i} \cdot \mathbf{v}) (\mathbf{v} \cdot \mathbf{v}) + (\mathbf{v} \cdot \mathbf{v}) (\mathbf{i}\cdot \mathbf{v})}{\mathbf{v} \cdot \mathbf{v}}\\
\mathbf{v} \cdot \mathbf{i_q}	&=0
\end{aligned}
\label{eq:v_iq}
\end{equation}

A observação apresentada na equação \ref{eq:v_ip} e \ref{eq:v_iq} trás uma implicação importante, na qual infere a ortogonalidade entre as correntes $\mathbf{i_p}$ e $\mathbf{i_q}$ no sistema, ou seja, $\mathbf{i_p} \cdot \mathbf{i_q}\equiv 0$. Deste modo, a seguinte relação pode ser obtida:

\begin{equation}
\begin{aligned}
i^2 &= \mathbf{i} \cdot \mathbf{i} = (\mathbf{i_p+\mathbf{i_q}}) \cdot (\mathbf{i_p}+\mathbf{i_q})\\
&= \mathbf{i_p} \cdot \mathbf{i_p} + \mathbf{i_q} \cdot \mathbf{i_q} + 2(\mathbf{i_p} \cdot \mathbf{i_q})\\
&= i_{\alpha p}^2 + i_{\beta p}^2 + i_{0 p}^2 + i_{\alpha q}^2 + i_{\beta q}^2 + i_{0q}^2\\
i^2 &= i_p^2+i_q^2
\end{aligned}
\end{equation}

Deste modo observa-se uma semelhança entre a teoria de potência PQ e a teoria apresentada por Fryze. Todavia deve ser lembrado que a demonstração anterior é válida para sistemas trifásicos com ou sem neutro, ao passo que a teoria de Fryze é utilizada para explicar os efeitos da potência em circuitos monofásicos.

\subsection{Potência Instantânea em coordenadas $abc$}

A teoria das potências instantâneas apresentada anteriormente é baseada em sistemas trifásicos cujas tensões $abc$ têm suas coordenadas transformadas para a referência $\alpha \beta 0$. Entretanto tal teoria não é limitada às coordenadas do sistema, sendo que a aplicação das definições e teoremas apresentadas anteriormente são válidas para o sistema trifásico baseado nas coordenadas $abc$. Deste modo a definição de tensão instantânea passa a compor o espaço vetorial na forma $\mathbf{v} = [v_a\;v_b\;v_c]^T$ onde as tensões $v_a$, $v_b$ e $v_c$ são as tensões das fases $a$, $b$ e $c$ de um sistema trifásico qualquer, respectivamente. O mesmo vale para a corrente $\mathbf{i} = [i_a\;i_b\;i_c]^T$, com as corrente $i_a$, $i_b$ e $i_c$ sendo as correntes nas linhas que compões as fases $a$, $b$ e $c$, respectivamente. Com isso as mesmas equações \ref{eq:Palphabeta0} e \ref{eq:q} já apresentadas podem ser aplicadas aqui, com a diferença que os valores dos vetores são referenciados nas coordenadas $abc$. Sendo assim:

\begin{equation}	%\label{eq:V}
\begin{aligned}
p &= \mathbf{v_{abc}} \cdot \mathbf{i_{abc}}\\
p &= v_{a}i_{a}+v_{b}i_{b}+v_{c}i_{c}
\end{aligned}
\label{eq:Palabc}
\end{equation}

\begin{equation}	%\label{eq:V}
\mathbf{q_{abc}} = 
\begin{bmatrix}
q_a\\
q_b\\
q_c
\end{bmatrix} = 
\mathbf{v_{abc}} \times \mathbf{i_{abc}}
\label{eq:qabc}
\end{equation}

Independentemente das coordenadas utilizadas os valores instantâneos das potências ativa e reativa são idênticos em todo espaço de tempo. Dessa forma as relações $p = \mathbf{v_{abc}} \cdot \mathbf{i_{abc}} = \mathbf{v_{\alpha \beta 0}} \cdot \mathbf{i_{\alpha \beta 0}}$ e $q_{\alpha \beta 0} = |\mathbf{q_{\alpha \beta 0}}|=|\mathbf{q_{abc}}|=q_{abc}$ são válidas. Além disso, as definições e propriedades de correntes ativa e reativa instantâneas são validas para as coordenadas $abc$.

Fisicamente a interpretação das potências nas coordenadas $abc$ é de mais fácil entendimento em comparação às coordenadas $\alpha \beta 0$, visto que as denotações são válidas para cada fase propriamente dita, sem a utilização da transformação de coordenadas. Porém o emprego da transformada de Clarke é de grande valia em razão de que esta separa as tensões e correntes $v_0$ e $i_0$ dos eixos $\alpha$ e $\beta$, de modo que a realização das potências instantâneas nessa coordenada é mais conveniente em se tratando de circuitos com a presença de sequencia zero. Além disso, a transformada de coordenadas apresenta-se vantajosa em sistemas trifásicos sem a presença de sequência zero visto que os cálculos matriciais são proferidos em matrizes de dimensão 2x2, visto que existem duas componentes de tensão ($v_{\alpha}$ e $v_{\beta}$) e corrente ($i_{\alpha}$ e $i_{\beta}$) apenas. Este fator torna a concepção de compensadores simplificada quando comparado à aplicação nas coordenadas $abc$. 

\subsection{Teoria PQ em Sistemas Trifásicos a Três Fios}\label{subsec:Teoria PQ em Sistema Trifásicos a Três Fios}

A teoria de potência instantânea apresentada anteriormente apresenta uma poderosa ferramenta para o estudo do comportamento de sistemas elétricos trifásicos genéricos. Esta teoria é baseada no domínio do tempo e pode ser aplicada em regime transitório ou permanente, em sistemas balanceados ou desbalanceados, trifásicos com ou sem a presença de neutro. A complexidade fica a cargo da abrangência das características presentes no sistema sob estudo. Para o caso específico de uso aeronáutico, certas particularidades nas características do sistema elétrico fazem com que a teoria de potência instantânea possa ser realizada com algumas simplificações sem perda de generalidade.

Dentre as principais características de um sistema aeronáutico, àquelas que proporcionam relevância no estudo da teoria da potência são:

\begin{itemize}[leftmargin=1.75cm,itemindent=0cm]
	\item Sistemas balanceados
	\item Desconsideração do fio neutro
\end{itemize} 

Os sistemas podem ser considerados balanceados visto que a geração de energia elétrica é proveniente de geradores que, por seus aspectos construtivos, possuem a geração de tensões senoidais balanceadas sem a presença de sequência negativa ou zero. Ainda, as normas aeronáuticas exigem que as cargas sejam distribuídas de maneira balanceada nas fases, de modo a não haver quedas de tensão nas fases evitando desbalancear as tensões do sistema trifásico no PCC.

Apesar da geração e distribuição elétrica em aeronaves com sistema trifásico ser baseada na presença do fio neutro, os equipamentos a qual a teoria da potência instantânea será implementada para realização da filtragem ativa são projetadas essencialmente a três fios ou com a desconsideração do neutro. Isto se deve ao fato de que as cargas trifásicas a qual apresentam não linearidades e, consequentemente, injeção de harmônicas no sistema são majoritariamente sem a presença do neutro. Para o caso em que o neutro faz-se presente a corrente nesta acaba por ser nula dado pelo equilíbrio das correntes nas linhas do circuito, cabendo a desconsideração deste na teoria da potência instantânea elucidada neste trabalho. As cargas trifásicas sem a presença de neutro são basicamente àquelas cuja entrada apresenta uma ponte de Graetz, como mostrado na Figura \ref{fig:Graetz.png}. Já as cargas não lineares com a conexão de neutro são baseadas na presença de transformadores cuja saída é conectada a uma ponte de elementos semicondutores. Para este último caso a corrente no neutro é nula e pode ser desconsiderada nos cálculos de potência instantânea. Um exemplo desta última carga é mostrada na Figura \ref{fig:12pulse.png}.

\begin{figure}[!htb] %Circuito real monofásico
	\centering
	\includegraphics[width=0.4\textwidth]{Cap3/Figuras/Graetz.png}
	\caption{Retificador trifásico por ponte de Graetz}
	\label{fig:Graetz.png}
\end{figure}

\begin{figure}[!htb] %Circuito real monofásico
	\centering
	\includegraphics[width=0.5\textwidth]{Cap3/Figuras/12pulse.png}
	\caption{Retificador trifásico com neutro}
	\label{fig:12pulse.png}
\end{figure}

Com as simplificações propostas nessa seção, os valores instantâneos de $v_o$ e $i_o$ tem valores nulos. Utilizando as definições propostas nas equações \ref{eq:Palphabeta0} e \ref{eq:q} levando em conta os valores $v_o=0$ e $i_o=0$, as potências instantâneas $p$ e $q$ são definidas a seguir:

\begin{equation}
p = \mathbf{v}\cdot \mathbf{i} = v_{\alpha}i_{\alpha}+v_{\beta}i_{\beta}
\label{eq:p}
\end{equation}

\begin{equation}
\mathbf{q} = \mathbf{v}\times \mathbf{i} =
\begin{bmatrix}
q_{\alpha}\\
q_{\beta}\\
q_0
\end{bmatrix} =
\begin{bmatrix}
0\\
0\\
v_{\alpha}i_{\beta}-v_{\beta}i_{\alpha}
\end{bmatrix} 
\label{eq:}
\end{equation}

Pela definição de potência instantânea reativa definida pela equação \ref{eq:qnorm}, o valor de $q$ é denotado por:

\begin{equation}
q = |\mathbf{q}|= q_0
\label{eq:}
\end{equation}

As potências instantâneas definidas podem ser combinadas em uma matriz, como mostrado na equação \ref{eq:pq_combined}. Nesta expressão estão presentes tanto a consideração da presença dos valores de corrente e tensão de sequência zero, como também os valores das potências $p$ e $q$ com a implicação que traz com a exclusão de $v_0$ e $i_0$ 

\begin{equation}
\begin{bmatrix}
p\\
q_{\alpha}\\
q_{\beta}\\
q_0
\end{bmatrix}
=
\begin{bmatrix}
v_{\alpha}		&	v_{\beta}	&	v_0\\
0				&	-v_0		&	v_{\beta}\\
v_{0}			&	0			& 	- v_{\alpha}\\
-v_{\beta}		&	v_{\alpha}	&	0
\end{bmatrix}
\begin{bmatrix}
i_{\alpha}\\
i_{\beta}\\
i_0
\end{bmatrix};
\qquad
\begin{aligned}
v_0&=0\\
i_0&=0
\end{aligned}
\Longrightarrow
\begin{bmatrix}
p\\
q
\end{bmatrix}
=
\begin{bmatrix}
v_{\alpha}		&	v_{\beta}\\
-v_{\beta}		&	v_{\alpha}
\end{bmatrix}
\begin{bmatrix}
i_{\alpha}\\
i_{\beta}
\end{bmatrix}
\label{eq:pq_combined}
\end{equation}

Outra maneira de definir as potências instantâneas baseadas na exclusão das componentes de sequencia zero $v_0$ e $i_0$ é a partir da transformada fasorial das tensões e correntes nas coordenadas $\alpha \beta 0$. Sendo os valores das tensões nos eixos $\alpha$ e $\beta$ defasados em $90^o$, as tensões e correntes fasoriais podem ser definidos a seguir:

\begin{equation}
\mathbf{v} = v_{\alpha}+jv_{\beta}
\label{eq:v_fasor}
\end{equation}

\begin{equation}
\mathbf{i} = i_{\alpha}+ji_{\beta}
\label{eq:i_fasor}
\end{equation}

Pela definição de potência aparente no espaço vetorial dada por $\mathbf{s}=\mathbf{v}\mathbf{i}^*$, onde ($^*$) denota o valor conjugado, a potência aparente em termos das tensões e correntes nas coordenadas $\alpha$ e $\beta$ é dado por:

\begin{equation}
\mathbf{s}=\mathbf{v}\mathbf{i}^* = (v_{\alpha} + jv_{\beta})(i_{\alpha} - ji_{\beta}) = (v_{\alpha}i_{\alpha}+v_{\beta}i_{\beta})+j(v_{\beta}i_{\alpha}-v_{\alpha}i_{\beta})
\label{eq:s_fasor}
\end{equation}

Pela própria definição de potência, os valores presentes no eixo real são definidos como sendo a potência ativa ao passo que as potências no eixo imaginário são definidas como as potências reativas. Deste modo, através da equação \ref{eq:s_fasor} os valores ativos e reativos são definidos por $p = v_{\alpha}i_{\alpha}+v_{\beta}i_{\beta}$ e $q = v_{\beta}i_{\alpha}-v_{\alpha}i_{\beta}$, ou também na forma:

\begin{equation}
\begin{bmatrix}
p\\
q
\end{bmatrix}=
\begin{bmatrix}
v_{\alpha}	&	v_{\beta}\\
v_{\beta}	&	-v_{\alpha}
\end{bmatrix}
\begin{bmatrix}
i_{\alpha}\\
i_{\beta}
\end{bmatrix}
\label{eq:pq}
\end{equation}

Nota-se uma inversão de sinal na potência instantânea reativa $q$ entre a teoria originalmente apresentada por \cite{Akagi1984} e exposta na equação \ref{eq:pq_combined} com a teoria da potência apresentada na expressão \ref{eq:pq}. Ambas as teorias são válidas quanto a aplicabilidade e no design de compensadores ativos, entretanto, segundo \cite{Akagi2007}, a teoria na equação \ref{eq:pq} apresenta um significado melhor aceito dado o conceito convencional que correntes em atraso (indutivo) apresentam sinal corretamente definidos com a utilização da equação \ref{eq:pq}.

\subsection{Significado Físico dos Parâmetros da Teoria PQ}\label{subsec:Significado Físico dos Parâmetros da Teoria PQ}

As definições apresentadas anteriormente são suficientes para elucidar os conceitos físicos que as potências instantâneas $p$ e $q$ apresentam. 

Como explicado anteriormente, o conceito de potência instantânea ativa advém do produto entre tensão e corrente as quais estão dispostos em eixos paralelos de um espaço vetorial, independentemente das coordenadas utilizadas. Deste modo, para um sistema trifásico com ou sem neutro, a potência ativa tem seu significado físico descrito pelo fluxo de energia por unidade de tempo entre dois subsistemas \cite{Akagi2007}. Cabe salientar que $p$ é descrito como uma função no tempo, sendo que o valor em determinados instantes representam o fluxo de potência instantânea. Esta definição pode diferir com o conceito clássico da teoria de potência que utiliza de valores médios para determinar a transferência de potência entre dois subsistemas. 

Analogamente à $p$, a potência reativa instantânea $q$ é definida como o produto de tensões e correntes perpendiculares no espaço vetorial, deste modo sua contribuição no fluxo de energia por unidade de tempo entre dois subsistemas é descrito como nulo. Dessa forma, o significado físico que descreve a potência reativa instantânea $q$ é o fluxo de energia por unidade de tempo que é trocado entre as fases do sistema sem que haja transferência entre subsistemas. Esta característica só é encontrada em sistemas trifásicos e não pode ser aplicado a circuitos monofásicos separadamente. A Figura \ref{fig:pqfisico.png} ilustra uma maneira de observar a presença das potências em um sistema trifásico qualquer. Em tal figura pode-se observar que em um determinado instante de tempo uma potência trifásica denominada $p_{3\phi}=p+p_0$ flui de um subsistema para outro, ao passo que a $q$ esta contido na troca de potência entre as fases, sem contribuir para o fluxo de energia entre subsistemas. Mesmo assim, deve-se lembrar de que a presença de $q$ faz com que as correntes de fase são acrescidas, como descrito anteriormente.

\begin{figure}[!htb] %Circuito real monofásico
	\centering
	\includegraphics[width=0.42\textwidth]{Cap3/Figuras/pqfisico.png}
	\caption{Circulação das potências instantâneas em um sistemas trifásico \cite{Akagi2007}}
	\label{fig:pqfisico.png}
\end{figure}

Para o melhor entendimento, considera-se o exemplo a seguir realizado com a utilização do circuito da Figura \ref{fig:3ph_ex.png}. Esse sistema é composto por um circuito trifásico a quatro fios alimentado uma carga de capacitores com capacitâncias $C_1$, $C_2$ e $C_3$.

\begin{figure}[!htb] %Circuito real monofásico
	\centering
	\includegraphics[width=0.45\textwidth]{Cap3/Figuras/3ph_ex.png}
	\caption{Circuito trifásico a quatro fios}
	\label{fig:3ph_ex.png}
\end{figure}

Primeiramente, considerando que a carga seja equilibrada, ou seja, os capacitores apresentam mesmo valor de capacitância, as potências instantâneas $p$ e $q$ em função do tempo são apresentadas nas figuras \ref{fig:p_bal.eps} e \ref{fig:q_bal.eps}, respectivamente. Para este caso pode-se observar que a transferência de potência ativa entre a fonte e a carga é nula, visto que os capacitores são carregados e descarregados de tal modo que o total de energia trocada entre a fonte e a carga é nulo. Já a potência reativa não é nula visto que existe uma corrente equilibrada entre as fases e estas apresentam ortogonalmente dispostas às tensões.

\begin{figure*}[!htb] %Circuito típico de um retificador de 12 pulsos com sua respectiva corrente de entrada
	\centering
	\begin{subfigure}[b]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Cap3/Figuras/p_bal.eps}
		\caption{Potência instantânea $p$ para carga balanceada} 
		\label{fig:p_bal.eps}
	\end{subfigure}%
	%	\hfill
	\begin{subfigure}[b]{0.48\textwidth}  
		\centering 
		\includegraphics[width=\textwidth]{Cap3/Figuras/q_bal.eps}
		\caption{Potência instantânea $q$ para carga balanceada}    
		\label{fig:q_bal.eps}
	\end{subfigure}%
	\caption{Potências instantâneas considerando $C_1=C_2=C_3$}
	\label{fig:pq_bal}
\end{figure*}

O segundo exemplo utiliza o mesmo circuito, porém com um capacitor $C_1$ com valor diferente de $C_2$ e $C_3$. Nesse caso o carregamento e descarregamento dos capacitores não serão balanceados, de modo a haver uma corrente instantânea nos terminais de $C_1$ diferentemente à $C_2$ e $C_3$. Com isso um fluxo de potência ativa instantânea existe entre a fonte e a carga, de modo que em um semi-cilo a fonte fornece energia e no semi-ciclo subsequente ocorre o oposto. Isto pode ser observado na Figura \ref{fig:p_desb.eps}. Entretanto ainda existem componentes de corrente dispostas perpendicularmente às tensões, havendo assim a presença de potência reativa $q$ sendo trocada entre as fases.

\begin{figure*}[!htb] %Circuito típico de um retificador de 12 pulsos com sua respectiva corrente de entrada
	\centering
	\begin{subfigure}[b]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Cap3/Figuras/p_desb.eps}
		\caption{Potência instantânea $p$ para carga desbalanceada} 
		\label{fig:p_desb.eps}
	\end{subfigure}%
	%	\hfill
	\begin{subfigure}[b]{0.48\textwidth}  
		\centering 
		\includegraphics[width=\textwidth]{Cap3/Figuras/q_desb.eps}
		\caption{Potência instantânea $q$ para carga desbalanceada}    
		\label{fig:q_desb.eps}
	\end{subfigure}%
	\caption{Potências instantâneas considerando $C_1\neq C_2=C_3$}
	\label{fig:pq_desb}
\end{figure*}
